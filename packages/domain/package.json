{
  "name": "@hkucc/domain",
  "version": "0.1.0",
  "private": true,
  "description": "The shared domain vocabulary of CONTEXT.md — Payload's generated types, and whatever both apps must agree on",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//payload": "A type-only dependency — nothing in this package has a runtime. payload-types.ts ends with a `declare module 'payload'` augmentation, which will not typecheck unless the module it augments resolves.",
  "devDependencies": {
    "payload": "3.88.0",
    "typescript": "^5"
  }
}
